/*double[] elements = new double[5];

Console.WriteLine("Enter a: ");
elements[0] = Convert.ToDouble(Console.ReadLine());
Console.WriteLine("Enter b: ");
elements[1] = Convert.ToDouble(Console.ReadLine());
Console.WriteLine("Enter c: ");
elements[2] = Convert.ToDouble(Console.ReadLine());


void quadratic_equation()
{
    elements[3] = (-(elements[1]) + (Math.Sqrt(elements[1] * elements[1] - 4 * elements[0] * elements[2]))) / (2 * elements[0]);
    elements[4] = (-(elements[1]) - (Math.Sqrt(elements[1] * elements[1] - 4 * elements[0] * elements[2]))) / (2 * elements[0]);
}

if ((elements[1] * elements[1] - 4 * elements[0] * elements[2]) >= 0)
{
    quadratic_equation();
}
else
{
    Console.WriteLine("D < 0");
    Environment.Exit(0);
}
Console.WriteLine("x1= " + elements[3]);
Console.WriteLine("x2= " + elements[4]);
*/